/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Voeg hier domeinen toe als je externe afbeeldingen gebruikt
    remotePatterns: [],
  },
};

module.exports = nextConfig;
